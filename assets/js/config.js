/* ============================================================
   root+ runtime config
   Fill these in to send signups to your Supabase project.
   Get them from Supabase → Project Settings → API.
   The anon key is safe to expose publicly — it's protected by the
   Row Level Security (insert-only) policy in supabase-setup.sql.
   Leave blank to keep demo mode (stores to the browser's localStorage).
   ============================================================ */
window.ROOTPLUS = {
  supabaseUrl: "",   // e.g. https://abcdefgh.supabase.co
  supabaseKey: ""    // the "anon public" API key
};
