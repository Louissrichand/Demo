# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Node (only if you add tooling later)
node_modules/
dist/
.cache/

# Env / secrets — never commit these
.env
.env.*
.dev.vars

# Local source materials (large / confidential — not needed to build the site)
Materials for Website/

# Local dev helper (hardcoded machine paths)
serve.ps1

# Text-editing worksheets (working files, not part of the site)
*.xlsx
